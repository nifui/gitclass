//For parsing the workflow, we can just have serde as the layer that converts from TOMl to structs.
//From there we can further classify the structs based on their prefix allowing us a DSL from TOML.
